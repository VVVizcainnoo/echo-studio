# 回声：我的 AI 翻唱制作人

定位：让不会使用音频软件的人，制作一段愿意保存或分享的个人翻唱。明星音色保留为未来授权合作方向。

用户：喜欢音乐、希望用自己的声音创作，但不熟悉音频软件的人。需求仍为假设，尚未完成用户研究。

核心流程：上传个人参考录音 → 确认声音使用 → 选择歌曲片段 → 设置偏好 → 素材检查 → 音色转换 → 混音 → 前后试听 → 调整或下载 → 反馈。

首版范围：一个个人音色、两段短素材、可追踪任务、失败重试、试听对比、下载。暂不做账号、支付、社区、公开音色市场或完整歌曲批量生成。

执行设计：单 Agent 使用允许的音频工具；Harness 负责参数校验、任务状态、取消、重试边界与记录。当前 demo 用确定性状态机替代模型决策，未连接大模型或声音转换 API。

当前可用：本地音频选择与解码、本人声音确认、示例档案、曲目选择、演示任务与取消、合成器旋律试听、音高与声部音量调整、WAV 下载、会话内反馈。

边界：示例旋律为程序合成，不是真人歌声，不使用上传音色。个人录音不上传或持久保存。外部录音棚图片加载会向图片提供方发起请求。页面关闭后录音和反馈不保留。

验证计划：邀请 3～5 人完成任务，记录完成率、卡点和保存意愿。接入真实生成后评估音色相似度、自然度、耗时、成功率和单次成本。尚无验证结果，不将演示数据视为真实质量证据。

交付目标：9 月 23 日完成可展示版本与复盘，9 月 24 日展示。零预算优先，先验证云端方案，不采购显卡。

异常流程：格式/大小/解码失败回到素材准备；执行失败保留输入供重试；取消使旧任务失效；修改音高、音量或曲目使旧结果失效。

后续接入：真实歌声转换适配器 → 音频质量评测 → 成本与超时控制 → 单 Agent 调参。未经接入的步骤不得声称真实执行。
